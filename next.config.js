/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // если у вас уже есть какие-то другие настройки, оставьте их
  output: 'export',
}

module.exports = nextConfig
